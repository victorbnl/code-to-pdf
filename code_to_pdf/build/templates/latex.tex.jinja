\documentclass[<= options.font_size =>]{article}

\usepackage{fancyvrb}

\usepackage{geometry}

\geometry{
    top=<= options.top_margin =>,
    bottom=<= options.bottom_margin =>,
    right=<= options.right_margin =>,
    left=<= options.left_margin =>
}

<% if options.paper %>
\geometry{paper=<= options.paper =>}
<% endif %>

<% if options.paper_width %>
\geometry{paperwidth=<= options.paper_width =>}
<% endif %>

<% if options.paper_height %>
\geometry{paperheight=<= options.paper_height =>}
<% endif %>

\usepackage[dvipsnames]{xcolor}
\definecolor{background}{HTML}{<= colors["background"] =>}
\definecolor{foreground}{HTML}{<= colors["foreground"] =>}

\pagecolor{background}
\makeatletter
\AtBeginDocument{\color{foreground}\global\let\default@color\current@color}
\makeatother

<% if not options.page_numbers %>
\pagenumbering{gobble}
<% endif %>

<= style_defs =>

\begin{document}
<= content =>
\end{document}
